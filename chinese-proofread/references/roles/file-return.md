# 文件任务的结束消息

候选、原文片段、修改提案和判断理由只写入本任务结果文件，不放入给主 Agent 的消息。按任务给定的 submit 命令提交成功后，最终消息只能是 `<任务ID> submitted`；无法继续时只能是 `<任务ID> blocked: <简短类别>`。任务 ID 从 open 返回的指令获取，不猜测。

提交成功后不再总结校对内容；失败类别只说明执行障碍，不引用原文。不向主 Agent 发送中间候选或理由。
